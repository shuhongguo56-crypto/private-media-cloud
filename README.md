# Private Media Cloud Entry

This public repository hosts only a static GitHub Pages entry for a private media cloud.

It may contain the latest public tunnel origin in `gateway.json`, but it must never contain private files, filenames, passwords, cookies, JWTs, database dumps, LAN addresses, or share passwords.
